{
  "name": "legion-sentry-backend",
  "version": "0.1.0-dev",
  "private": true,
  "description": "Legion Sentry G1 appliance API",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "node test/run.js",
    "lint": "node --check src/app.js && node --check src/server.js && node --check src/services/devices/pointDiscovery.js && node --check src/routes/devices.js && node --check src/middleware/errorHandler.js"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^7.1.0",
    "legion-sentry": "file:..",
    "morgan": "^1.10.0",
    "node-bacnet": "^0.2.4",
    "serialport": "^12.0.0"
  }
}
